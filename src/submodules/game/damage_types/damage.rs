#[derive(Debug)]
pub enum AttackTypes {
    Physical,
    Magic,
}

#[derive(Debug)]
pub enum ElementTypes {
    Fire,
    Ice,
    Earth,
    Wind,
    Electric,
    Blood,
    NoElement,
}